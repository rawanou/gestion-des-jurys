<odoo>

    <record id="view_jury_decision_tree" model="ir.ui.view">
        <field name="name">gestion.jury.decision.tree</field>
        <field name="model">gestion.jury.decision</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="jury_id"/>
                <field name="decision"/>
            </tree>
        </field>
    </record>

    <record id="view_jury_decision_form" model="ir.ui.view">
        <field name="name">gestion.jury.decision.form</field>
        <field name="model">gestion.jury.decision</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="jury_id"/>
                        <field name="decision"/>
                        <field name="commentaire"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_gestion_jury_decision" model="ir.actions.act_window">
        <field name="name">Décisions du Jury</field>
        <field name="res_model">gestion.jury.decision</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="menu_gestion_jury_decision"
              name="Décisions du Jury"
              parent="menu_gestion_jurys_root"
              action="action_gestion_jury_decision"/>

</odoo>
